# Kiro IDE files
.kiro/
.claude/
# Production environment file (contains secrets)
.env.prod

# TLS certificates — never commit private keys or cert bundles
/nginx/certs/*.pem
/nginx/certs/*.crt
/nginx/certs/*.key
